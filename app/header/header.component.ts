import { Component, OnInit } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: "header",
    templateUrl: "header.html"
})
export class HeaderComponent {
    constructor() {}
}